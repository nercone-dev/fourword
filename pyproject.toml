[build-system]
requires = ["uv_build>=0.9.5,<0.10.0"]
build-backend = "uv_build"

[project]
name = "fourword"
version = "1.5.2"
readme = "README.md"
requires-python = ">=3.9"
dependencies = []

[project.urls]
'GitHub' = 'https://github.com/nercone-labs/fourword/'

[project.scripts]
fourword = "fourword.cli:main"
